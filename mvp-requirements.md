# A 股量化数据底座 MVP 需求文档

## 1. 文档目标

本文定义量化金融数据底座第一阶段 MVP 的产品范围、功能需求、验收标准和交付节奏。

MVP 的目标不是做最大最全的金融数据库，而是先打通 A 股中低频量化研究的核心闭环：

> 主数据 -> 行情 -> Point-in-Time 基本面 -> 指数/行业历史 -> 因子 -> 回测取数 SDK。

## 2. MVP 定位

### 2.1 一句话定位

面向 A 股量化研究和回测的 Point-in-Time 数据底座。

### 2.2 核心价值

- 让量化研究员可以稳定获取行情、财务、指数、行业、因子和交易约束数据。
- 让回测只能看到历史时点当时可见的数据，降低未来函数风险。
- 让研究、因子计算、回测和后续实盘使用同一套数据口径。
- 让每个数据结果可以追溯到来源、批次、版本和入库时间。

### 2.3 不做的事情

MVP 阶段暂不做：

- 面向普通投资者的金融终端。
- 完整新闻资讯平台。
- 全量研报平台。
- 高频 tick 和 Level-2。
- 期货、期权、港股、美股等多资产。
- 完整实盘交易系统。
- 复杂前端投研工作台。

## 3. 目标用户和使用场景

### 3.1 目标用户

- 量化研究员。
- 量化开发工程师。
- 数据工程师。
- 回测平台工程师。
- 投资经理和策略负责人。

### 3.2 核心场景

#### 场景一：研究员构造股票池

研究员希望在某个历史日期获取沪深 300、中证 500、中证 1000 或自定义股票池，并排除停牌、ST、上市未满指定天数的股票。

系统需要支持：

- 获取历史指数成分。
- 获取历史行业分类。
- 获取历史 ST 状态。
- 获取历史停复牌状态。
- 获取上市日期和退市日期。

#### 场景二：研究员批量取行情

研究员希望获取某个股票池的日线或分钟线行情，用于计算动量、反转、波动率、换手率和流动性因子。

系统需要支持：

- 批量股票。
- 批量日期。
- 前复权、后复权、不复权。
- 分钟线。
- 停牌和涨跌停状态。

#### 场景三：研究员使用 Point-in-Time 财务数据

研究员希望在 2021-06-30 回测时，只使用截至当日已经披露的数据，而不是今天数据库中已经修正后的最终财务数据。

系统需要支持：

- 按 `asof_date` 查询。
- 区分报告期、披露时间、入库时间和版本。
- 支持财报重述。
- 支持业绩预告和业绩快报。

#### 场景四：因子生产

量化开发希望定义一个因子，声明输入数据，批量计算历史值，记录代码版本和数据版本，并检查覆盖率、缺失率和未来函数风险。

系统需要支持：

- 因子定义。
- 因子版本。
- 因子值存储。
- 因子质量指标。
- 因子取数接口。

#### 场景五：回测引擎取数

回测引擎需要在每个调仓日获取当时可交易股票池、价格、交易约束、因子、财务数据和行业暴露。

系统需要支持：

- 高性能批量查询。
- 明确的时点语义。
- 统一的 Python SDK。
- 可审计的数据版本。

## 4. MVP 成功标准

### 4.1 产品成功标准

- 可以支撑至少 3 类中低频 A 股策略完整回测：
  - 价值/质量因子策略。
  - 动量/反转策略。
  - 指数增强策略。
- 研究员可以通过 Python SDK 完成从股票池到行情、财务、因子和交易约束的完整取数。
- 财务、指数成分和行业分类支持历史时点查询。
- 每次数据查询可以返回或追踪数据版本。

### 4.2 数据成功标准

- A 股股票主数据覆盖率达到 99.9% 以上。
- 日线行情交易日完整率达到 99.9% 以上。
- 分钟线行情在已授权数据范围内交易日完整率达到 99.5% 以上。
- 交易日历准确覆盖沪深北交易所。
- 财报数据支持报告期、披露时间、入库时间和修订版本。
- 指数成分和行业分类支持历史版本。

### 4.3 性能成功标准

MVP 阶段性能目标：

- 单股票 10 年日线查询 P95 小于 300ms。
- 1000 只股票 5 年日线批量查询 P95 小于 5s。
- 单指数历史成分查询 P95 小于 500ms。
- 1000 只股票单日因子横截面查询 P95 小于 1s。
- Python SDK 支持返回 pandas DataFrame。

### 4.4 工程成功标准

- 所有核心数据集有采集批次记录。
- 所有核心数据集有质量检查。
- 所有核心数据表有字段口径文档。
- 所有 API 有入参、出参、错误码和版本说明。
- 关键查询有审计日志。

## 5. 功能需求

### 5.1 证券主数据

#### REQ-MASTER-001 证券唯一标识

系统必须为每个证券生成稳定的 `security_id`，所有业务表通过 `security_id` 关联，不能只依赖证券代码。

验收标准：

- 同一只证券代码变更后 `security_id` 不变。
- 不同交易所、不同资产类型的相同代码不会冲突。
- 可以通过代码和日期反查对应 `security_id`。

#### REQ-MASTER-002 代码和简称历史

系统必须记录证券代码和简称历史。

验收标准：

- 支持按任意交易日查询当日有效代码和简称。
- 支持记录开始日期、结束日期、来源和版本。

#### REQ-MASTER-003 上市退市状态

系统必须记录上市、暂停上市、恢复上市、退市、退市整理等状态。

验收标准：

- 支持按交易日判断股票是否在可投资范围内。
- 支持筛选上市未满 N 天股票。

### 5.2 交易日历

#### REQ-CALENDAR-001 交易日历

系统必须维护沪深北交易所交易日历。

验收标准：

- 支持判断某日是否交易日。
- 支持获取前一交易日和后一交易日。
- 支持获取日期区间内所有交易日。

### 5.3 行情数据

#### REQ-MARKET-001 日线行情

系统必须支持 A 股日线行情。

字段包括：

- 开盘价。
- 最高价。
- 最低价。
- 收盘价。
- 前收盘价。
- 成交量。
- 成交额。
- VWAP。
- 换手率。
- 涨停价。
- 跌停价。
- 停牌标记。

验收标准：

- 支持多股票、多日期批量查询。
- 支持前复权、后复权、不复权。
- 支持按宽表和长表返回。

#### REQ-MARKET-002 分钟线行情

系统必须支持 A 股分钟线行情。

验收标准：

- 支持 1 分钟级别。
- 支持按股票、日期区间和时间段查询。
- 支持返回 pandas DataFrame。
- 支持断点检测。

#### REQ-MARKET-003 复权因子

系统必须记录复权因子历史。

验收标准：

- 可用于计算前复权和后复权价格。
- 复权因子变化可追溯到分红送转事件。
- 复权因子异常跳变有告警。

#### REQ-MARKET-004 停复牌和涨跌停

系统必须支持历史停复牌和涨跌停价格。

验收标准：

- 回测时可以判断某日某股是否可买、可卖。
- 支持不同板块涨跌幅制度。
- 支持 ST 和非 ST 差异。

### 5.4 Point-in-Time 基本面

#### REQ-PIT-001 财务三表

系统必须支持资产负债表、利润表、现金流量表 Point-in-Time 查询。

验收标准：

- 每个字段保留报告期、披露时间、入库时间和版本。
- 同一报告期多次修订时保留多个版本。
- `asof_date` 查询不会返回未来披露数据。

#### REQ-PIT-002 主要财务指标

系统必须支持常用财务指标。

第一批指标包括：

- 营业收入。
- 归母净利润。
- 扣非归母净利润。
- 毛利率。
- 净利率。
- ROE。
- ROA。
- 资产负债率。
- 经营现金流。
- 每股收益。

验收标准：

- 支持按报告期和 as-of 时点查询。
- 支持 TTM、单季和累计口径标记。

#### REQ-PIT-003 业绩预告和快报

系统必须支持业绩预告和业绩快报。

验收标准：

- 记录预告类型。
- 记录利润上下限。
- 记录同比变化上下限。
- 支持按公告时点查询。

### 5.5 指数和行业

#### REQ-INDEX-001 指数成分历史

系统必须支持指数成分历史和权重历史。

验收标准：

- 支持按任意日期查询指数成分。
- 支持获取成分股生效日期和结束日期。
- 支持沪深 300、中证 500、中证 1000、全 A 等核心股票池。

#### REQ-INDUSTRY-001 行业分类历史

系统必须支持申万和中信行业历史。

验收标准：

- 支持一级、二级、三级行业。
- 支持按历史日期查询当时行业归属。
- 支持行业分类调整版本。

### 5.6 因子平台

#### REQ-FACTOR-001 因子定义

系统必须支持因子定义和元数据管理。

验收标准：

- 因子有唯一 `factor_id`。
- 因子记录频率、类型、描述、负责人、代码版本和状态。
- 因子声明输入数据依赖。

#### REQ-FACTOR-002 因子值存储

系统必须支持日频因子值存储。

验收标准：

- 支持股票、日期、因子三维查询。
- 支持批量横截面查询。
- 支持数据版本和代码版本追踪。

#### REQ-FACTOR-003 因子质量检查

系统必须支持第一版因子质量检查。

指标包括：

- 覆盖率。
- 缺失率。
- 极值比例。
- 均值。
- 标准差。
- 横截面分布。

### 5.7 Python SDK

#### REQ-SDK-001 SDK 初始化

系统必须提供 Python SDK，支持 token 鉴权和基础配置。

验收标准：

- 可配置服务地址。
- 可配置超时。
- 可配置返回格式。

#### REQ-SDK-002 核心取数接口

第一批 SDK 必须支持：

- `get_security_master`
- `get_trading_calendar`
- `get_price`
- `get_adjustment_factor`
- `get_trading_constraints`
- `get_fundamental_asof`
- `get_index_members_asof`
- `get_industry_asof`
- `get_factor`
- `get_universe`

验收标准：

- 返回 pandas DataFrame。
- 错误信息清晰。
- 参数校验明确。

### 5.8 数据质量和运维

#### REQ-QUALITY-001 数据质量检查

每个核心数据集必须有质量检查任务。

验收标准：

- 检查结果入库。
- 失败触发告警。
- 支持按数据集、日期和批次查看质量状态。

#### REQ-AUDIT-001 查询审计

核心 API 查询必须记录审计日志。

验收标准：

- 记录用户、接口、参数摘要、时间、状态和数据版本。
- 敏感参数不明文记录。

## 6. 非功能需求

### 6.1 可用性

MVP 阶段目标：

- 工作日核心服务可用性 99.5%。
- 批处理失败可重跑。
- 数据修正可追溯。

### 6.2 可扩展性

系统应预留：

- 多资产扩展。
- tick 和 Level-2 扩展。
- 多租户权限。
- 私有化部署。
- 实时行情链路。

### 6.3 安全性

MVP 必须支持：

- Token 鉴权。
- API 权限控制。
- 查询审计。
- 数据集授权边界记录。
- 导出限制。

### 6.4 兼容性

API 和 SDK 应遵守：

- 新增字段兼容。
- 废弃字段至少保留一个版本周期。
- 错误码稳定。
- 日期、证券代码和枚举格式稳定。

## 7. 交付物

MVP 第一批交付物：

- 数据仓库基础环境。
- 证券主数据。
- 交易日历。
- 日线行情。
- 分钟线行情。
- 复权因子。
- 停复牌和涨跌停。
- PIT 财务三表。
- 业绩预告和快报。
- 指数成分历史。
- 行业分类历史。
- 因子定义和因子值。
- 数据质量检查。
- Python SDK。
- API 契约。
- 字段口径文档。

## 8. 里程碑计划

### 8.1 M1：基础数据地基

周期：第 1 个月。

交付：

- 证券主数据。
- 交易日历。
- 数据源和批次表。
- 原始数据湖目录规范。
- PostgreSQL 和 ClickHouse 初始化。

### 8.2 M2：行情闭环

周期：第 2 个月。

交付：

- 日线行情。
- 分钟线行情。
- 复权因子。
- 停复牌。
- 涨跌停。
- `get_price`。
- `get_trading_constraints`。

### 8.3 M3：PIT 基本面

周期：第 3 个月。

交付：

- 财务三表。
- 主要财务指标。
- 业绩预告。
- 业绩快报。
- `get_fundamental_asof`。
- 未来函数检测第一版。

### 8.4 M4：指数、行业、股票池

周期：第 4 个月。

交付：

- 指数成分历史。
- 指数权重历史。
- 申万行业历史。
- 中信行业历史。
- `get_index_members_asof`。
- `get_industry_asof`。
- `get_universe`。

### 8.5 M5：因子平台

周期：第 5 个月。

交付：

- 因子定义。
- 因子日频值。
- 基础技术因子。
- 基础财务因子。
- 因子质量检查。
- `get_factor`。

### 8.6 M6：回测验证和试点

周期：第 6 个月。

交付：

- 对接一个回测引擎。
- 跑通三类策略。
- 数据质量日报。
- 查询审计。
- 试点用户反馈闭环。

## 9. 验收策略

### 9.1 数据验收

- 抽样核对核心字段。
- 与至少两个数据源交叉校验关键行情和财务字段。
- 校验交易日完整性。
- 校验复权价格连续性。
- 校验 PIT 查询不会返回未来数据。

### 9.2 API 验收

- 核心接口参数校验。
- 批量查询性能测试。
- 错误码测试。
- 权限测试。
- 兼容性测试。

### 9.3 策略验收

必须跑通：

- 价值/质量策略。
- 动量/反转策略。
- 指数增强策略。

每个策略需要输出：

- 股票池构造过程。
- 数据版本。
- 因子版本。
- 回测结果。
- 异常数据清单。

## 10. 待确认事项

- 首批数据源和授权范围。
- 分钟线历史长度。
- 财务数据供应商和公告披露时间口径。
- 指数成分和权重授权来源。
- 行业分类授权来源。
- SDK 第一版是否只支持 Python。
- 是否优先支持私有化部署。
- 是否需要接入现有回测引擎。
